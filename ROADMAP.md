# 项目重构路线图

这份路线图用于规划基于原项目的长期重构和产品化工作。

时间按每周投入约 6–8 小时估算，可以根据实际情况顺延。每个阶段完成验收标准后再进入下一阶段，不要求一次性完成全部计划。

## 总体目标

- 保留并稳定现有的简历编辑、模板切换、国际化和配置导入导出能力。
- 建立清晰、可迁移的简历数据模型。
- 提升编辑器的保存、撤销、预览和移动端体验。
- 保证 A4 打印和 PDF 输出质量。
- 逐步补齐测试、自动化部署和项目文档。
- 在核心功能稳定后，再评估是否从 Gatsby 迁移到 Vite。

## 时间线总览

| 阶段          | 时间         | 目标                             |
| ------------- | ------------ | -------------------------------- |
| 0. 建立基线   | 第 1 周      | 确认原项目可运行、可构建、可部署 |
| 1. 项目整理   | 第 2 周      | 明确项目边界、配置和代码规范     |
| 2. 数据模型   | 第 3–4 周    | 固化简历数据结构和数据迁移机制   |
| 3. 编辑器体验 | 第 5–6 周    | 完善自动保存、撤销重做和编辑流程 |
| 4. 模板与输出 | 第 7–8 周    | 稳定模板渲染、打印和 PDF 输出    |
| 5. 测试与发布 | 第 9–10 周   | 建立测试、CI 和发布流程          |
| 6. 技术升级   | 第 11 周以后 | 根据需要升级依赖或迁移构建工具   |

## 0. 建立基线：第 1 周

- [x] 保存当前 fork 的初始版本和 Git 标签（`baseline-v1.1.5`）。
- [x] 使用 pnpm 安装依赖并启动本地开发环境。
- [x] 验证简历编辑、模板切换、语言切换和配置导入导出。
- [x] 验证只读模式、编辑模式和 URL 参数功能。
- [x] 验证生产构建和 GitHub Pages 部署流程。
- [x] 记录当前已知问题、浏览器兼容性问题和旧项目行为（见 `BASELINE.md`）。
- [x] 移除或替换原作者的统计 ID、默认链接和项目身份信息。

### 阶段验收

能够从全新的环境完成安装、开发、构建和部署，并且知道哪些行为是原项目已有问题。

## 1. 项目整理：第 2 周

- [x] 整理 README，补充项目定位、开发方式和部署方式。
- [x] 明确项目名称、站点地址、仓库地址和版权信息。
- [x] 统一使用 pnpm 的开发、构建和发布脚本。
- [x] 检查环境变量、Google Analytics 配置和 GitHub Pages 路径配置。
- [x] 为核心模块补充类型，减少隐式 `any`。
- [x] 统一组件、工具函数、数据文件和页面的目录职责。
- [x] 建立 issue 或 TODO 清单，记录暂不处理的问题。

### 阶段验收

新贡献者只阅读 README 就能完成本地运行，并且不会误用原作者的项目配置。

## 2. 数据模型：第 3–4 周

- [x] 定义统一的 `Resume` 数据结构。
- [x] 将基本信息、工作经历、教育经历、项目、技能和自定义模块分开建模。
- [x] 为数据增加 `schemaVersion` 字段。
- [x] 定义字段默认值和空数据行为。
- [x] 增加导入 JSON 的校验和错误提示。
- [x] 增加旧版本 JSON 到新版本 JSON 的迁移函数。
- [x] 将 localStorage、URL 参数和远程数据读取封装成独立适配器。
- [x] 明确数据导入、导出、保存和渲染之间的边界。

### 阶段验收

同一份简历数据可以被不同模板渲染，导入旧配置不会导致页面崩溃，并且数据结构有明确的版本升级路径。

## 3. 编辑器体验：第 5–6 周

- [ ] 增加自动保存和最近一次保存状态提示。
- [ ] 增加撤销和重做。
- [ ] 优化模块拖拽排序和删除操作。
- [ ] 为必填字段和格式错误增加表单校验。
- [ ] 支持自定义模块的新增、编辑、排序和删除。
- [ ] 增加“恢复默认内容”前的确认操作。
- [ ] 优化编辑器在手机和平板上的布局。
- [ ] 处理刷新、关闭页面和恢复草稿等场景。

### 阶段验收

用户连续编辑一份简历时不会轻易丢失内容，常见操作可以撤销，手机端也能完成主要编辑流程。

## 4. 模板与输出：第 7–8 周

- [ ] 定义模板渲染接口，确保模板只负责展示数据。
- [ ] 统一主题颜色、字体、间距和页面尺寸配置。
- [ ] 明确 A4 页面宽高、边距和打印样式。
- [ ] 处理长文本、长列表和模块跨页问题。
- [ ] 验证单页和多页简历的分页效果。
- [ ] 验证中英文混排、头像缺失和图片加载失败场景。
- [ ] 增加打印前预览或打印提示。
- [ ] 为现有模板补充截图或快照基线。
- [ ] 评估是否需要新增模板，优先修复已有模板的一致性问题。

### 阶段验收

常见简历内容在 Chrome、Edge 和 Safari 中打印结果稳定，单页和多页 PDF 都不会出现明显截断、重叠或空白页。

## 5. 测试与发布：第 9–10 周

- [ ] 为数据默认值、导入导出和数据迁移增加单元测试。
- [ ] 为 URL 参数、语言切换和模板切换增加测试。
- [ ] 增加一条完整的端到端流程：打开项目、编辑内容、切换模板、导出配置。
- [ ] 将 TypeScript 类型检查加入构建流程。
- [ ] 将格式检查加入提交或 CI 流程。
- [ ] 增加 GitHub Actions：安装、检查、构建。
- [ ] 增加预览环境或部署前检查。
- [ ] 为发布流程补充版本号和变更记录。

### 阶段验收

提交代码后可以自动发现类型、格式和构建问题，核心数据逻辑有回归保护，发布过程可以重复执行。

## 6. 技术升级：第 11 周以后

这部分是可选项，应在核心功能稳定后进行。

- [ ] 评估 React、TypeScript、Ant Design 和 Gatsby 的升级成本。
- [ ] 先升级开发工具和类型依赖，再升级运行时依赖。
- [ ] 分阶段处理 Gatsby 版本升级，不要和业务重构混在同一个提交中。
- [ ] 评估从 Gatsby 迁移到 Vite + React 的收益和成本。
- [ ] 如果迁移到 Vite，先保持页面和数据行为不变，再调整构建方式。
- [ ] 清理不再使用的依赖、插件和旧配置。
- [ ] 重新验证 SEO、静态资源路径、GitHub Pages 子路径和部署缓存。

### 阶段验收

升级或迁移后，已有简历数据、URL、模板、打印效果和部署地址仍然可用。

## 后续候选功能

完成核心路线后，再从下面选择真正有需求的功能：

- [ ] 简历版本管理。
- [ ] 多份简历管理。
- [ ] 分享链接和只读链接。
- [ ] 更多导出格式，例如 Markdown 或 DOCX。
- [ ] 自定义字体和更多主题。
- [ ] 云端同步或账号系统。
- [ ] 模板市场。
- [ ] 简历内容检查和质量提示。

## 每次开发前的检查

- [ ] 这次改动解决的是具体问题，而不是单纯改变技术栈。
- [ ] 是否会影响已有 JSON 数据和 URL 参数。
- [ ] 是否需要增加数据迁移或兼容逻辑。
- [ ] 是否需要补充测试或截图基线。
- [ ] 是否能拆成一个容易回滚的小提交。
