# 待办与已知限制

本清单记录已确认但不属于当前“项目整理”阶段的问题。新问题应注明影响和计划阶段，避免散落在代码注释中。

## 数据与编辑器（阶段 2–3）

- 修复编辑态切换语言或模板时未保存内容可能丢失的问题。
- 增加自动保存状态、撤销重做和恢复默认内容前的确认。
- 改善移动端编辑能力；当前只保证查看体验。

## 模板与输出（阶段 4）

- 验证长文本、多页、中英文混排及头像加载失败时的 A4 打印结果。
- 为 Chrome、Edge 和 Safari 建立截图或打印基线。
- 移除模板缩略图和 Roboto 字体对外部 CDN 的依赖。

## 工程与发布（阶段 5–6）

- 为导入导出、URL 参数、语言和模板切换补充自动化测试。
- 将类型检查、格式检查和生产构建加入独立 CI 检查。
- 评估 Gatsby 2、React 17 和相关旧插件的升级或迁移成本。
- 移除 `@mdx-js/util` 临时版本覆盖；当前覆盖是 Gatsby 2 可运行基线所需。

## 外部依赖

- 只读远程加载依赖 GitHub Raw 可用性，以及 `{user}/{user}/{branch}/resume.json` 的仓库约定。
- 本地直接发布依赖维护者对目标 GitHub 仓库的写权限。
